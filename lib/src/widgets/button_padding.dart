import 'package:flutter/material.dart';

class ButtonPadding extends StatelessWidget {

  static const double padding = 16;
  @override
  Widget build(BuildContext context) => Container(height: padding);
}